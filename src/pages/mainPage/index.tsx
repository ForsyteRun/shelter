import { BackgroundWrapper, Header, Present } from "@/components/layout";

const MainPage = () => {
  return (
    <BackgroundWrapper>
      <Header />
      <Present />
    </BackgroundWrapper>
  );
};

export default MainPage;
