import { BackgroundWrapper, Header } from "@/components/layout";

const MainPage = () => {
  return (
    <BackgroundWrapper>
      <Header />;
    </BackgroundWrapper>
  );
};

export default MainPage;
